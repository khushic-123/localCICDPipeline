version: 2.1

jobs:
  maven-test:
    docker:
      - image: cimg/openjdk:17.0-browsers

    # LT_USERNAME and LT_ACCESS_KEY are still expected from the CircleCI UI/Context.

    steps:
      - checkout

      - restore_cache:
          keys:
            - maven-{{ checksum "pom.xml" }}
            - maven-

      - run:
          name: Verify index.html exists
          command: |
            echo "📂 Current directory: $(pwd)"
            ls -al
            if [ ! -f index.html ]; then
              echo "❌ index.html not found in root! Ensure it's checked out."
              exit 1
            fi

      - run:
          name: Debug LT credentials
          command: |
            echo "LT_USERNAME is not empty: ${LT_USERNAME:+YES}"
            echo "LT_ACCESS_ACCESS is not empty: ${LT_ACCESS_KEY:+YES}"

      - run:
          name: Start Local HTTP Server (Using 'serve')
          command: |
            echo "🛠️ Installing 'serve' for stable HTTP server..."
            npm install -g serve
            
            echo "🚀 Starting 'serve' on port 5500..."
            # Use 'serve' (which is more robust than Python's simple server)
            # -l 5500 sets the port, -s enables single-page application mode (safe fallback)
            serve -l 5500 -s . > serve.log 2>&1 &
            
            # Wait for the server to spin up
            sleep 5
            
            echo "Verifying local server access..."
            # Curl 127.0.0.1 to check local reachability
            curl -I http://127.0.0.1:5500/index.html
            
            echo "✅ Local server started."

      - run:
          name: Start LambdaTest Tunnel (with 443 fix and polling)
          command: |
            echo "🔽 Downloading LambdaTest Tunnel binary..."
            wget https://downloads.lambdatest.com/tunnel/v3/linux/64bit/LT_Linux.zip
            unzip LT_Linux.zip
            chmod +x LT
            
            echo "🚇 Starting LambdaTest Tunnel..."
            # Added --sshConnType over_443 and start in background
            ./LT --user $LT_USERNAME --key $LT_ACCESS_KEY --tunnelName myLocalTunnel --verbose --sshConnType over_443 > lt.log 2>&1 &
            
            sleep 5 # Small initial pause for logging to start
            
            echo "⏳ Waiting for tunnel to connect..."
            TIMEOUT=90
            for i in $(seq 1 $TIMEOUT); do
              if grep -q "You can start testing now" lt.log; then
                echo "✅ LambdaTest Tunnel is ready after $i seconds!"
                break
              fi
              sleep 1
            
              if [ $i -eq $TIMEOUT ]; then
                echo "❌ Tunnel failed to connect within $TIMEOUT seconds."
                tail -n 30 lt.log
                exit 1
              fi
            done
            echo "✅ Tunnel log preview (Successful):"
            tail -n 30 lt.log

      - run:
          name: Run Maven tests
          command: mvn -B clean test

      - run:
          name: Display Test Report on Failure
          command: |
            echo "--- Displaying Surefire Error Report ---"
            cat target/surefire-reports/TEST-org.example.LocalHtmlSeleniumTest.xml || true
            echo "----------------------------------------"
          when: always

      - run:
          name: Stop LambdaTest Tunnel and HTTP Server
          command: |
            echo "🛑 Stopping LambdaTest Tunnel..."
            pkill LT || true
            echo "🛑 Stopping HTTP Server..."
            # CRITICAL: Kill the 'serve' process instead of the Python process
            pkill -f 'serve -l 5500' || true

      - save_cache:
          paths:
            - .m2/repository
          key: maven-{{ checksum "pom.xml" }}

      - store_test_results:
          path: target/surefire-reports

      - store_artifacts:
          path: target

workflows:
  version: 2
  test-workflow:
    jobs:
      - maven-test


