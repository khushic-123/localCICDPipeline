version: 2.1

jobs:
  maven-test:
    # 1. Correct image with Java 17 and pre-installed browsers
    docker:
      - image: cimg/openjdk:17.0-browsers

    # ❌ IMPORTANT: Removed the redundant 'environment' block.
    #    LT_USERNAME and LT_ACCESS_KEY must be set in the CircleCI UI/Context
    #    and are now automatically available as shell variables.

    steps:
      - checkout

      - restore_cache:
          keys:
            - maven-{{ checksum "pom.xml" }}
            - maven-

      - run:
          name: Verify index.html exists
          command: |
            echo "📂 Current directory: $(pwd)"
            ls -al
            if [ ! -f index.html ]; then
              echo "❌ index.html not found in root! Ensure it's checked out."
              exit 1
            fi

      # ✅ DEBUG STEP: Use this to ensure your secrets are loaded.
      - run:
          name: Debug LT credentials
          command: |
            echo "LT_USERNAME is not empty: ${LT_USERNAME:+YES}"
            echo "LT_ACCESS_ACCESS is not empty: ${LT_ACCESS_KEY:+YES}"

      - run:
          name: Start Local HTTP Server
          command: |
            echo "🚀 Starting local server on port 5500..."
            python3 -m http.server 5500 &
            # Wait briefly for the Python server process to start
            sleep 2
            echo "✅ Local server started."

      - run:
          name: Start LambdaTest Tunnel (with 443 fix and polling)
          command: |
            echo "🔽 Downloading LambdaTest Tunnel binary..."
            wget https://downloads.lambdatest.com/tunnel/v3/linux/64bit/LT_Linux.zip
            unzip LT_Linux.zip
            chmod +x LT
            
            echo "🚇 Starting LambdaTest Tunnel..."
            # ✅ FIX: Added --sshConnType over_443 to bypass firewall issues
            ./LT --user $LT_USERNAME --key $LT_ACCESS_KEY --tunnelName myLocalTunnel --verbose --sshConnType over_443 > lt.log 2>&1 &
            
            echo "⏳ Waiting for tunnel to connect..."
            # ✅ IMPROVEMENT: Reliable polling loop (up to 90 seconds)
            for i in $(seq 1 90); do
              if grep -q "Tunnel is ready" lt.log; then
                echo "✅ LambdaTest Tunnel is ready after $i seconds!"
                break
              fi
              sleep 1
              if [ $i -eq 90 ]; then
                echo "❌ Tunnel failed to connect within 90 seconds."
                tail -n 30 lt.log # Show more log for debugging
                exit 1
              fi
            done

      - run:
          name: Run Maven tests
          # Assuming you have the correct dependencies in pom.xml
          command: mvn -B clean test

      - run:
          name: Stop LambdaTest Tunnel
          # Use pkill and verify it stopped for reliable cleanup
          command: |
            echo "🛑 Stopping LambdaTest Tunnel..."
            pkill LT || true 

      - save_cache:
          paths:
            - .m2/repository
          key: maven-{{ checksum "pom.xml" }}

      - store_test_results:
          path: target/surefire-reports

      - store_artifacts:
          path: target

workflows:
  version: 2
  test-workflow:
    jobs:
      - maven-test






